# Database Configuration (Dedicated Product Service Database)
# This service uses a dedicated PostgreSQL database following the database-per-service pattern
# In Docker: Use postgres-product:5432 (internal Docker network communication)
# For local development outside Docker: Use localhost:5433 (external mapped port)
DB_HOST=postgres-product
DB_PORT=5432
DB_NAME=product_database
DB_USER=product_user
DB_PASSWORD=product_pass

# Elasticsearch Configuration
ELASTICSEARCH_URL=http://localhost:9200

# Application Configuration
APP_ENV=development

